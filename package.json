{
  "name": "hainspector",
  "version": "1.1.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "vercel-build": "npx --yes vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
